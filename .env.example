# .env.example — example file (no real key is provided)
# Copy this file to .env and fill in your own variables if you have the full version.
# --- OpenAI Mode (optional) ---
OPENAI_API_KEY=

# --- Enable conversation persistence with SQLite ---
ENABLE_PERSISTENCE=true

# --- Number of messages kept in memory per session (UI) ---
MEMORY_MAX_TURNS=20
